/// Network API client skeleton — Phase 9+ will implement remote calls.
///
/// The network layer exists architecturally but is not required for MVP.
/// Core calendar functionality must never depend on this client.
library;

/// Placeholder for the HTTP API client.
class ApiClient {
  const ApiClient();

  // Phase 9+ implementation.
}
