/// Notification scheduler — Phase 7 will implement all scheduling logic.
library;

/// Schedules local notifications for all four notification categories.
///
/// Implementations must isolate all scheduling logic from UI code.
abstract interface class NotificationScheduler {
  // Phase 7 implementation.
}
