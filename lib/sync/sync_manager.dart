/// Sync manager skeleton — cloud sync is NOT part of MVP.
///
/// Architecture is extensible but sync will not be implemented
/// until a future phase beyond MVP.
library;

/// Manages synchronization between local database and remote API.
class SyncManager {
  const SyncManager();
  // Future implementation.
}
