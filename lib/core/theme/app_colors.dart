/// Semantic color tokens for the Mīzān Calendar app.
///
/// All colors are defined here and referenced by the rest of the app.
/// Never use raw hex values outside this file.
library;

import 'package:flutter/material.dart';

/// Brand seed color (deep teal-green, evocative of Islamic art).
const Color kColorSeed = Color(0xFF1A6B5A);

/// Islamic event accent color.
const Color kColorIslamicEvent = Color(0xFF4CAF82);

/// Personal event accent color.
const Color kColorPersonalEvent = Color(0xFF5C6BC0);

/// Prayer time accent color.
const Color kColorPrayer = Color(0xFF8B6914);

/// Today highlight background.
const Color kColorTodayBackground = Color(0xFF1A6B5A);

/// Today highlight foreground.
const Color kColorTodayForeground = Color(0xFFFFFFFF);

/// Selected day highlight background.
const Color kColorSelectedBackground = Color(0xFFD4EDE7);

/// Hijri date text color (secondary).
const Color kColorHijriText = Color(0xFF4A7A6E);

/// Error color.
const Color kColorError = Color(0xFFBA1A1A);

/// Surface color (light).
const Color kColorSurfaceLight = Color(0xFFF6FBF8);

/// Surface color (dark).
const Color kColorSurfaceDark = Color(0xFF0E1F1C);
